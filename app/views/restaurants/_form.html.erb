<%= form_for @restaurant  do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :address %>
	<%= f.text_field :address %>


	<%= f.label :cuisine %>
	<%= f.text_field :cuisine %>

	<%= f.submit buttonText %>
<% end %>